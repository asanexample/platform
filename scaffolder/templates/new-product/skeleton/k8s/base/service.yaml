apiVersion: v1
kind: Service
metadata:
  name: app-${{ values.team }}-${{ values.product }}
spec:
  type: ClusterIP
  selector:
    app: app-${{ values.team }}-${{ values.product }}
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
