# Prod progressive-delivery extras (ADR-056). The base is a plain single-Service Rollout (fast, preview-safe);
# prod adds the second Service the chosen strategy needs + a ready-to-enable metric gate. The Rollout strategy
# and (for canary) the weighted HTTPRoute are patched in kustomization.yaml.
{%- if values.deployStrategy == 'bluegreen' %}
# Blue/green: the PREVIEW service targets the green pods before cutover (Argo Rollouts injects the
# rollouts-pod-template-hash into the selector). The active service is the base Service; the HTTPRoute already
# points at it, so no route patch is needed — cutover is a selector swap, no Gateway-API plugin involved.
apiVersion: v1
kind: Service
metadata:
  name: app-${{ values.team }}-${{ values.product }}-preview
spec:
  type: ClusterIP
  selector:
    app: app-${{ values.team }}-${{ values.product }}
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
{%- else %}
# Canary: the CANARY service targets the new-revision pods (Argo Rollouts injects the
# rollouts-pod-template-hash). The base Service is the stable service; the HTTPRoute is patched to a weighted
# stable+canary pair (kustomization.yaml) and the Gateway-API plugin shifts the weights per step.
apiVersion: v1
kind: Service
metadata:
  name: app-${{ values.team }}-${{ values.product }}-canary
spec:
  type: ClusterIP
  selector:
    app: app-${{ values.team }}-${{ values.product }}
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
{%- endif %}
---
# Metric gate (ADR-056 D5/W8c) — the canary step in kustomization.yaml references this between weight bumps:
# the rollouts controller queries the hub Mimir for the new version's HTTP success rate and ABORTS the rollout
# (auto-rollback) if it misses. A PROMETHEUS provider (queried by the controller) — NOT a `job` provider, which
# Kyverno restrict-images would deny in an enforced namespace.
apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
metadata:
  name: app-${{ values.team }}-${{ values.product }}-canary-gate
spec:
  metrics:
    - name: success-rate
      interval: 1m
      # Fail (and auto-rollback) if the success rate misses twice in a row.
      failureLimit: 2
      provider:
        prometheus:
          # The hub Mimir read route (observability-mimir spoke_ingest query path, W8c). The hub force-SETS the
          # X-Scope-OrgID tenant, so no header is needed and the query is scoped to this cluster's own metrics.
          # NB: this is the prod STAGE's metric source — prod runs on the preprod cluster today; when a dedicated
          # prod cluster lands, point this at that cluster's Mimir read route.
          address: https://preprod-mimir.aws.refplat.org/prometheus
          query: |
            sum(rate(http_server_request_duration_seconds_count{
              k8s_namespace_name="{{ args.namespace }}", http_response_status_code!~"5.."}[1m]))
            /
            sum(rate(http_server_request_duration_seconds_count{
              k8s_namespace_name="{{ args.namespace }}"}[1m]))
      successCondition: "len(result) == 0 || result[0] >= 0.95"
  args:
    - name: namespace
---
# Error-budget FREEZE (ADR-056 W11 / ADR-054) — a PRE-FLIGHT gate, run once at the very start of the rollout
# (before any traffic shifts), querying THIS env's SLO burn rate. If the service is already burning its error
# budget too fast (an active incident / recent unreliability), the check FAILS and the rollout is FROZEN (aborts)
# rather than pushing change into an already-shaky service. Distinct from the canary success-rate gate above:
# that watches the NEW version mid-rollout; THIS asks "is the service healthy enough to deploy at all?".
# The per-env SLO is authored in observability-mimir (app_slos); sloth_id = <namespace>-availability. No SLO data
# yet (brand-new env) → passes (never blocks the first deploy). Threshold 2 = burning >=2x the budget; tunable.
apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
metadata:
  name: app-${{ values.team }}-${{ values.product }}-budget-freeze
spec:
  args:
    - name: namespace
  metrics:
    - name: error-budget-burn
      count: 1 # one-shot pre-flight check, not a continuous watch
      provider:
        prometheus:
          address: https://preprod-mimir.aws.refplat.org/prometheus
          query: |
            slo:current_burn_rate:ratio{sloth_id="{{args.namespace}}-availability"}
      successCondition: "len(result) == 0 || result[0] < 2"
