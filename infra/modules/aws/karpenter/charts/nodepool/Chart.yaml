apiVersion: v2
name: karpenter-nodepool
description: Karpenter EC2NodeClass + NodePool (rendered from values). Deployed via helm so the CRs don't need
  the CRD present at plan time (unlike kubernetes_manifest).
type: application
version: 0.1.0
