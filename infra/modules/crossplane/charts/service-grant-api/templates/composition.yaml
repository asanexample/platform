{{/*
The ServiceGrant Composition carries an inline go-template whose {{ }} must NOT be processed by Helm. It is
shipped as a raw file and emitted verbatim via .Files.Get so the gotemplating delimiters reach Crossplane
intact (the same pattern as environment-api / agent-api).
*/ -}}
{{ .Files.Get "files/composition.yaml" }}
