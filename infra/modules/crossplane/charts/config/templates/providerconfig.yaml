# EKS Pod Identity credentials (ADR-041): no SA annotation, no OIDC. The provider pods assume the
# provisioning IAM role via the Pod Identity association created in Terraform. Cluster-scoped (v2 API).
apiVersion: aws.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: {{ .Values.providerConfigName }}
spec:
  credentials:
    source: PodIdentity
