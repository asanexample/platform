# Sample XAgent for the render test — the triage copilot (XAgent #1, ADR-080/082). obs-read, propose-only.
apiVersion: platform.refplat.org/v1beta1
kind: XAgent
metadata:
  name: triage-copilot
spec:
  product: triage-copilot
  placement:
    cluster: platform
  model:
    provider: bedrock
    id: us.anthropic.claude-sonnet-4-6
  obsRead: true
  autonomy:
    mode: propose-only
  trigger:
    kind: alertmanager-webhook
  lifecycle:
    phase: active
