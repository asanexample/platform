# Kill-switch fixture (ADR-082 D7): lifecycle.phase suspended must remove the PodIdentityAssociation (hard-stop
# Bedrock) while retaining the namespace/SA/role slot. Otherwise identical to triage-copilot.yaml.
apiVersion: platform.refplat.org/v1beta1
kind: XAgent
metadata:
  name: triage-copilot
spec:
  team: platform
  product: triage-copilot
  placement:
    cluster: platform
  model:
    provider: bedrock
    id: us.anthropic.claude-sonnet-4-6
  obsRead: true
  autonomy:
    mode: propose-only
  lifecycle:
    phase: suspended
