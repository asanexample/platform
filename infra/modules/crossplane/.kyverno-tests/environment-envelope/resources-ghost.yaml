# Team 'ghost' has no projected Team CR → team-must-exist flags.
apiVersion: platform.refplat.org/v1beta1
kind: XEnvironment
metadata: { name: ghostenv }
spec: { team: ghost, product: svc, stage: dev, tier: standard }
