# MUST FAIL: posture outside the enum.
apiVersion: platform.refplat.org/v1beta1
kind: AccessGrant
metadata: { name: bad-posture }
spec: { target: { team: alpha, product: demo }, subject: user:x, posture: admin }
