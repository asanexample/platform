# MUST FAIL: isolation.compute outside the ladder enum.
apiVersion: platform.refplat.org/v1beta1
kind: XEnvironment
metadata: { name: bad-isolation }
spec: { team: alpha, product: demo, stage: dev, isolation: { compute: dedicated-galaxy } }
