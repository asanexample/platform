# MUST FAIL: tenancy.mode=dedicated without tenancy.customer (the x-kubernetes-validations CEL rule).
apiVersion: platform.refplat.org/v1alpha2
kind: XTenant
metadata:
  name: dedicated-missing-customer
spec:
  team: payments
  name: payments-api
  environment: prod
  tier: pci
  tenancy:
    mode: dedicated
