# MUST FAIL: envelope.allowedTiers contains a value outside the enum.
apiVersion: platform.refplat.org/v1alpha2
kind: Team
metadata:
  name: bad-tier
spec:
  ssoGroup: Dev-bad
  envelope:
    allowedTiers: [standard, ultra]
    allowedEnvironments: [preprod]
    quotaCap: { cpu: "8", memory: 16Gi, pods: 40 }
