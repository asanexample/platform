apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: disallow-irsa-annotation-cross-team
  labels:
    {{- include "kpp.labels" . | nindent 4 }}
  annotations:
    policies.kyverno.io/title: Disallow Environment IRSA Annotation
    policies.kyverno.io/category: Security, Multi-Tenancy
    policies.kyverno.io/severity: high
    policies.kyverno.io/subject: ServiceAccount
    policies.kyverno.io/description: >-
      Environment workloads get AWS access via platform-managed EKS Pod Identity (an association binding a
      specific namespace+ServiceAccount to the team's role — ADR-041), NOT via IRSA. The
      eks.amazonaws.com/role-arn annotation is therefore never needed in an environment namespace, and a
      forged annotation is a cross-team privilege-escalation vector — so it stays denied (a backstop;
      Pod Identity does not use this annotation).
spec:
  background: true
  failurePolicy: {{ .Values.failurePolicy }}
  rules:
    - name: deny-irsa-annotation
      match:
        any:
          - resources:
              kinds:
                - ServiceAccount
              {{- include "kpp.environmentNamespaceSelector" . | nindent 14 }}
      validate:
        failureAction: {{ .Values.validationFailureAction }}
        message: "Environment ServiceAccounts may not carry an eks.amazonaws.com/role-arn annotation — environment AWS access is via platform-managed EKS Pod Identity, not IRSA (ADR-041)."
        deny:
          conditions:
            any:
              - key: "{{`{{ request.object.metadata.annotations.\"eks.amazonaws.com/role-arn\" || '' }}`}}"
                operator: NotEquals
                value: ""
