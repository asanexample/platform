# Named ServiceAccount the app runs as. AWS access (if any) is bound out-of-band by a platform-managed
# EKS Pod Identity association (ADR-041) — NO eks.amazonaws.com/role-arn annotation here.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: demo
  namespace: team-alpha
  labels:
    app.kubernetes.io/name: demo
