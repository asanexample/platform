# Module Name

One-line description of what this module creates and why you'd use it.

## Usage

```hcl
module "example" {
  source = "../module_name"

  create = true

  # Required
  name                = "my-resource"
  resource_group_name = "my-rg"
  location            = "eastus"

  # Optional
  environment = "dev"

  tags = {
    Environment = "dev"
    ManagedBy   = "Terragrunt"
  }
}
```

## Examples

### Disabled module

```hcl
module "example" {
  source = "../module_name"
  create = false
}
```

### Advanced configuration

```hcl
module "example" {
  source = "../module_name"

  # Show a meaningfully different configuration here
}
```

<!-- BEGIN_TF_DOCS -->
<!-- auto-generated by terraform-docs — do not edit below this line -->
<!-- END_TF_DOCS -->

## Dependencies

- [naming](../naming) — Resource name generation
- [resource_group](../resource_group) — Resource group

## Notes

- Include only non-obvious information: gotchas, security considerations, integration patterns.
- If removing a note wouldn't confuse a future reader, don't write it.
