# THROWAWAY — Phase 1 GitOps loop test (provision + prune). Will be removed immediately after verification.
apiVersion: platform.refplat.org/v1alpha1
kind: XTenant
metadata:
  name: gamma
spec:
  team: gamma
  hostnames:
    - demo-gamma.preprod.aws.refplat.org
  apps:
    demo:
      repoPath: k8s/preprod
      preview: false
  aws:
    serviceAccount: app-gamma
    policyStatements: []
