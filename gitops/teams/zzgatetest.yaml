# Throwaway team to exercise teams-gate — now COMPLIANT (should pass).
apiVersion: platform.refplat.org/v1alpha2
kind: Team
metadata:
  name: zzgatetest
spec:
  ssoGroup: Dev-zzgatetest
  envelope:
    allowedTiers: ["standard"]
    allowedEnvironments: ["dev", "test"]
    quotaCap:
      cpu: "8"
      memory: 16Gi
      pods: 40
    maxDedicatedZones: 0
