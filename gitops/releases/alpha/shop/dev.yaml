# Control-plane deployed-digest record (ADR-071). CI-owned (the promote App bumps it); the per-Product
# ApplicationSet injects the digest, so the app repo's overlay stays :placeholder and its main is untouched.
# Seeded with the showcase's current digest at the ADR-071 cutover.
apiVersion: platform.refplat.org/v1beta1
kind: Release
metadata: {name: alpha-shop-dev}
spec:
  environmentRef: alpha-shop-dev
  services:
    web: {digest: "sha256:0de25d8ae1e7e1c0b5b9b1158e932fedb1121fa736dc693ba3230004cb55c475"}
